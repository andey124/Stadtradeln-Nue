# Copilot Instructions

> **Note:** This is a template. Update each section as the project takes shape.

## Project Overview

<!-- Describe what this project does. E.g.:
A web app for tracking cycling distances as part of the Stadtradeln campaign.
Users log rides, join teams, and compete on a leaderboard. -->

## Tech Stack

<!-- List the key technologies used, e.g.:
- Runtime: Node.js 20 / Python 3.12 / .NET 9
- Framework: React / Vue / Angular / ASP.NET / Django
- Database: PostgreSQL / SQLite / MongoDB
- Styling: Tailwind CSS / SCSS
- Auth: ...
-->

## Build, Test & Lint

```bash
# Install dependencies
# e.g.: npm install / pip install -r requirements.txt

# Run dev server
# e.g.: npm run dev

# Build for production
# e.g.: npm run build

# Run all tests
# e.g.: npm test

# Run a single test file
# e.g.: npm test -- path/to/file.test.ts

# Lint
# e.g.: npm run lint
```

## Architecture

<!-- Describe the high-level structure once files exist.
E.g.:
- `src/` — application source code
  - `components/` — reusable UI components
  - `pages/` — route-level page components
  - `lib/` — shared utilities and API clients
- `server/` — backend API (Express)
- `db/` — migrations and schema definitions
-->

## Key Conventions

<!-- Document patterns specific to this codebase that aren't obvious from reading a single file.
E.g.:
- All API routes are defined in `src/routes/` and auto-registered via `src/routes/index.ts`
- Database queries use the `db` helper from `src/lib/db.ts`, never raw SQL strings
- Component files are PascalCase; utility files are camelCase
- Translations live in `public/locales/` (i18n via react-i18next)
-->
